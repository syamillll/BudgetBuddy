<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BudgetBuddy" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/white</item> <!-- Status bar color -->
        <item name="android:statusBarColor">@color/colorOnPrimary</item> <!-- Status bar color -->
        <item name="android:windowBackground">@color/white</item> <!-- Background color -->
    </style>

    <style name="Theme.BudgetBuddy" parent="Base.Theme.BudgetBuddy" />
</resources>
